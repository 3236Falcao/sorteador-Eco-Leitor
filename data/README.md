# Pasta local — NÃO versionada (exceto este aviso e o exemplo fictício)
Esta pasta guarda a lista REAL de alunos no computador do professor.

- Nunca commite arquivos reais aqui.
- O `.gitignore` bloqueia `data/` por padrão.
- Exceção versionada: `data/alunos.exemplo.csv` (nomes fictícios).
- Formato esperado: turma,nivel,nome
